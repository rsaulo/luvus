import raw from '../assets/icons/luvus-logo.svg?raw';

/**
 * The compact, fixed-colour brand mark shared by every site navigation.
 * Keeping one raw import prevents landing pages and documentation from drifting.
 */
export const NAV_MARK = raw;
