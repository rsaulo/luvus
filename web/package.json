{
  "name": "luvus-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "bridge",
    "packages/*"
  ],
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "scripts": {
    "build": "npm run build -w @luvus/uhp-client && npm run build -w @luvus/web-app && npm run build -w @luvus/web-bridge",
    "build:native": "npm run build -w @luvus/uhp-client && npm run build:native -w @luvus/web-app",
    "check": "npm run check -w @luvus/uhp-client && npm run check -w @luvus/web-app && npm run check -w @luvus/web-bridge",
    "dev": "node scripts/dev.mjs",
    "dev:stop": "node scripts/stop.mjs",
    "test": "npm run build && npm run test -w @luvus/uhp-client && npm run test -w @luvus/web-app && npm run test -w @luvus/web-bridge",
    "test:integration": "npm run build && node bridge/test/live-integration.mjs",
    "test:native": "node scripts/test-native.mjs",
    "test:live": "node scripts/test-live.mjs",
    "start": "npm run start -w @luvus/web-bridge"
  }
}
